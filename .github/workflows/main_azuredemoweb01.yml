name: Build and deploy Node.js app to Azure Web App - azuredemoweb01

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Clean npm cache
        run: npm cache clean --force

      # 安装包含 optional 的依赖，避免 lightningcss 缺二进制
      - name: Install dependencies (include optional)
        run: npm ci --include=optional

      # 强制重建 lightningcss 原生模块（容错）
      - name: Rebuild lightningcss
        run: npm rebuild lightningcss --force || true

      - name: Build Next.js app
        env:
          NODE_OPTIONS: --max_old_space_size=4096
        run: npm run build

      # 构建完成后裁掉 dev 依赖，减小部署体积
      - name: Prune dev dependencies
        run: npm prune --omit=dev

      # 打包成 zip（保留 node_modules/.next）
      - name: Zip artifact (with node_modules)
        run: |
          zip -r app.zip . \
            -x ".git/*" ".github/*" "tests/*" \
               "**/.vscode/*" "**/.DS_Store"

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: prebuilt-zip
          path: app.zip
          retention-days: 7

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: prebuilt-zip

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'azuredemoweb01'
          slot-name: 'production'
          package: 'app.zip'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_A77B9AB3A0B44B10AD979B6804554E26 }}
